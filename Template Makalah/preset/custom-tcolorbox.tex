\makeatletter
\ltx@ifpackageloaded{tcolorbox}{

%===========================%
%     Textbox Tcolorbox     %
%===========================%

% --- Block Quote ---
\newtcolorbox{blockquote}{
    parbox=false,
    boxrule=0pt,
    frame hidden,
    colback=graybg,
    enhanced,
    sharp corners,
    borderline west={2pt}{0pt}{black}
}

% --- Block Quote Indent ---
\newtcolorbox{indentbquote}{
    size=minimal,
    parbox=false,
    boxrule=0pt,
    frame hidden,
    colback=graybg,
    enhanced,
    sharp corners,
    left=\parindent,
    right=\parindent,
    top=10pt,
    bottom=10pt,
    borderline west={2pt}{0pt}{black}
}

% --- Text Box ---
\newtcolorbox{textbox}{
    parbox=false,
    breakable,
    sharp corners,
    colback=white,
    boxrule=0.5pt
}

% --- LST Codeblock ---
\NewTCBListing{lstcode}{ !O{} }{%
    arc=0mm,
    outer arc=0mm,
    boxrule=0.5pt,
    colback=graybg,
    colframe=graybr,
    left=4mm,
    right=4mm,
    top=3mm,
    bottom=3mm,
    breakable,
    listing only,
    listing options={
        basicstyle=\footnotesize\ttfamily,
        aboveskip=0pt,
        belowskip=0pt,
        frame=none,
        breaklines=true,
        xleftmargin=0em,
        xrightmargin=0em,
        #1
    }
}

\AtBeginDocument{%
    \NewTCBListing[blend into=listings]{lstcodecap}{ O{} m O{} }{%
        arc=0mm,
        outer arc=0mm,
        boxrule=0.5pt,
        colback=graybg,
        colframe=graybr,
        left=4mm,
        right=4mm,
        top=3mm,
        bottom=3mm,
        breakable,
        listing and comment,
        listing options={
            basicstyle=\footnotesize\ttfamily,
            aboveskip=0pt,
            belowskip=0pt,
            frame=none,
            breaklines=true,
            xleftmargin=0pt,
            xrightmargin=0pt,
            caption={#2},
            #1
        },
        title={#2},
        detach title,
        coltitle=black,
        listing above* comment,
        comment={\centering\small\tcbtitle\vspace{18pt}},
        #3
    }%
}

%===========================%
%     Theorem Tcolorbox     %
%===========================%

\tcbset{
    theoremboxstyle/.style={
        parbox=false,
        arc=0pt,
        outer arc=0pt,
        boxrule=0.5pt,
        titlerule=0.5pt,
        before skip=12pt, after skip=12pt,
        colback=white,
        colframe=graybr,
        colbacktitle=graybg,
        coltitle=black,
        breakable,
        lines before break=2,
        label separator={}%:
    }
}

% --- Teorema (Theorem) ---
\newtcbtheorem[use counter=theorem, number freestyle={\noexpand\thetheorem}]{theorembox}{\Teorema}{
    theoremboxstyle,
    fonttitle=\bfseries,
    fontupper=\itshape
}{}%thm

% --- Lema (Lemma) ---
\newtcbtheorem[use counter=lemma, number freestyle={\noexpand\thetheorem}]{lemmabox}{\Lema}{
    theoremboxstyle,
    fonttitle=\bfseries,
    fontupper=\itshape
}{}%lem

% --- Definisi (Definition) ---
\newtcbtheorem[use counter=definition, number freestyle={\noexpand\thetheorem}]{definitionbox}{\Definisi}{
    theoremboxstyle,
    fonttitle=\bfseries
}{}%def

% --- Contoh (Example) ---
\newtcbtheorem[use counter=example, number freestyle={\noexpand\thetheorem}]{examplebox}{\Contoh}{
    theoremboxstyle,
    fonttitle=\itshape
}{}%ex

}{
    \typeout{Template Info: tcolorbox not used.}
}
\makeatother